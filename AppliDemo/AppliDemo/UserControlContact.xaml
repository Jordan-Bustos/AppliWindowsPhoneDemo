<UserControl x:Class="AppliDemo.UserControlContact"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="480" d:DesignWidth="480"
             
    x:Name="userControlContact"
    xmlns:my="clr-namespace:AppliDemo"    
    >

    <Grid x:Name="LayoutRoot" Background="{StaticResource PhoneChromeBrush}">
        <StackPanel Orientation="Vertical" VerticalAlignment="Center">
            <TextBlock FontWeight="Bold" Text="Contact:" />
            <my:UserControlLbTxt x:Name="FieldPersonne"/>
            <my:UserControlLbLink x:Name="FieldEMail"/>
            <my:UserControlLbLink x:Name="FieldWebSite"/>
        </StackPanel>
    </Grid>
</UserControl>
